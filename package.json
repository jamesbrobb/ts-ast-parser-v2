{
  "name": "ts-ast-parser-v2",
  "version": "0.0.1-beta.1",
  "description": "",
  "main": "public-api.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p tsconfig.json && copyfiles ./package.json ./README.md dist/@jamesbenrobb/ts-ast-parser-v2/",
    "build:prod": "tsc -p tsconfig.prod.json && copyfiles ./package.json ./README.md dist/@jamesbenrobb/ts-ast-parser-v2/",
    "cd:dist": "cd dist/@jamesbenrobb/ts-ast-parser-v2",
    "publish:beta": "npm version prerelease --preid=beta && npm run build:prod && npm run cd:dist && npm publish --access public --tag beta",
    "publish:major": "npm version major && npm run build:prod && npm run cd:dist && npm publish --tag latest",
    "publish:minor": "npm version minor && npm run build:prod && npm run cd:dist && npm publish --tag latest",
    "publish:patch": "npm version patch && npm run build:prod && npm run cd:dist && npm publish --tag latest"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.30",
    "copyfiles": "^2.4.1",
    "tsx": "^4.15.6",
    "typescript": "^5.4.3"
  },
  "dependencies": {
    "deepmerge": "^4.3.1"
  }
}
